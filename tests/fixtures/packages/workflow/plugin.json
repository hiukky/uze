{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/plugin.schema.json",
  "name": "workflow",
  "description": "Canonical package proving invocation policy: `review` exists once as a Skill, with a user-only invoke policy (the semantics previously modeled as a separate Command)."
}
