# dashboard-app

Run `npm run test:unit` before committing.

Use Claude's plan mode before touching anything under `src/billing/`.

Components live under `src/components/`, one directory per component with
a colocated `.test.tsx` file.
