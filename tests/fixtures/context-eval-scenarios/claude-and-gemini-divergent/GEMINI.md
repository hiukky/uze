# dashboard-app

Before finishing, run `npm run test:unit` and confirm it's green.

Ask before installing any new dependency — this project has a strict
bundle-size budget enforced in CI.

Use `/memory show` if you're unsure what context is currently loaded.
