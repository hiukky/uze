//! Projection tests: how canonical resources are *named*, routed and
//! physically represented per harness — exposure naming, invocation
//! labels/policy, generated packages and shared skill roots.

mod invocation;
mod naming;
mod shared_roots;
mod worktree_policy;
