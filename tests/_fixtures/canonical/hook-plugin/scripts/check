#!/bin/sh
# Fixture handler on the portable contract: the hook context arrives as
# HOOK_* environment, the decision leaves as an exit code (0 allow, 3 deny
# with the reason on stderr).
case "$HOOK_COMMAND" in
  *secrets*)
    echo "blocked by protect-env" >&2
    exit 3 ;;
esac
exit 0
