---
title: Overview
description: The terminal workspace where your agents run, in spaces, tabs and panes that survive closing it
---

Run `uze` with no arguments and you get the **workspace**: one terminal that
holds all your agents.

```bash
uze
```

<Callout type="info">
  The workspace is **experimental**. Its layout and shortcuts may still change.
</Callout>

## Spaces, tabs, panes

| | What it is |
| --- | --- |
| **Space** | A directory you work in, usually a project. `Alt+S` adds one |
| **Tab** | A session inside it: an agent (`Alt+N`) or a shell (`Ctrl+T`) |
| **Pane** | The live terminal of the selected tab |

A shell you open while an agent is selected belongs to that agent: it starts in
the agent's checkout and closes with it.

## Nothing is lost when you close it

Your agents keep running when you quit the workspace, and are exactly where you
left them the next time you run `uze`. After a reboot, uze starts each agent
again in the checkout it was working in, back in the conversation it was having.

## When an agent finishes

An agent that finishes while you are looking elsewhere leaves a check on its
tab. To hear it too:

```bash
uze config notification on      # ring the terminal bell when an agent finishes
uze config notification silent  # never
```

## Management

`Ctrl+O` switches to management and back: your plugins, marketplaces, agents'
setup, appearance and shortcuts, all with the mouse.

<Cards>
  <Card title="Running agents" href="/docs/workspace/agents" />
  <Card title="Extensions" href="/docs/workspace/extensions" />
  <Card title="Keys" href="/docs/configuration/keys" />
</Cards>
