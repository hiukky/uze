{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/plugin.schema.json",
  "name": "hook-plugin",
  "description": "Conformance portable hooks: one group whose first handler denies the shell tool when the call mentions `secrets`, and whose second handler denies with its own marker \u2014 so the deny scenario proves first-deny-wins by that marker's absence. A parallel native-matcher group intercepts the conformance MCP tool for the OpenCode vertical, whose runtime exposes no shell tool."
}
