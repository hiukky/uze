#!/bin/sh
# Always denies. Its reason is the conformance evidence marker for
# first-deny-wins across handlers of one group.
echo "first-handler-denied [tool=$HOOK_TOOL native=$HOOK_TOOL_NATIVE]" >&2
exit 3
