[package]
name = "uze-core"
version.workspace = true
edition = "2024"
rust-version = "1.97"
description = "Harness-agnostic package, capability, lifecycle, and integration contracts for UZE"
license = "MIT"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
toml = "0.9"
