"""The stub server the example key in mcp.json runs.

Swap it for your real server — or delete the key in mcp.json and this
file with it. An MCP server that points at nothing is worse than one
that says so.
"""


def main() -> None:
    print("hello-world MCP stub: replace me", flush=True)


if __name__ == "__main__":
    main()
