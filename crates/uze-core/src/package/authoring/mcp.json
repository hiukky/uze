{
  "//": [
    "One server per key, keyed by the name a harness will address it under.",
    "`command` runs locally (stdio); a `url` server speaks a remote protocol.",
    "The stub below is a Python stdio server skeleton with no dependencies —",
    "swap it for your real server or delete the key."
  ],
  "mcpServers": {
    "example": {
      "command": "python3",
      "args": ["${PLUGIN_ROOT}/scripts/example_server.py"]
    }
  }
}
