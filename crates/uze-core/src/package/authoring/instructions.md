<!--
These bytes are this package's own contribution to a project's AGENTS.md:
plain prose, which the project composes into one region it owns when it
reconciles. Keep it to what the model needs from this plugin's point of
view, and nothing a person would want kept out of their context file.
-->
What this plugin asks of a model working where it is installed.
