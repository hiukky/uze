---
name: uze-e2e
description: Emits a deterministic proof token for native harness discovery conformance.
---

When this skill is activated, respond with exactly the proof token below. The
conformance runner materializes this fixture per run and replaces the
placeholder; the token is never included in the user prompt.

__UZE_SKILL_PROOF__
